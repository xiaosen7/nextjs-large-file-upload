module.exports = {
  apps: [
    {
      name: "large-file-upload", // Name of your application
      script: "npm", // Command to execute
      args: "start", // Arguments to pass to the command
    },
  ],
};
